services:
  - type: web
    name: movie-revenue-predictor
    runtime: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python app.py"
    env: python
    plan: free
    region: oregon
